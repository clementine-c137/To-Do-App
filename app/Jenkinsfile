pipeline {
  agent any
    //docker { image 'node:12-alpine' }
    
  
  stages {
    stage('Build') {
      steps {
        sh 'ls -l'
        sh 'pwd'
        sh 'docker build -t tali-app:latest app/Dockerfile'
      }
    }
  }
}
